#!/bin/bash
# nvm --version
# vpm --version
ME='whoami'
echo "WHOAMI = $ME "
echo "$PATH"
uname -a
gcloud --version || true
docker --version || true
terraform --version || true
java --version || true
kubectl version || true